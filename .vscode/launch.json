{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Ada Program",
            "request": "launch",
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Ada : Compile and Run"
        }
    ]
}